# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShortcutRecorderCrash' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShortcutRecorderCrash
  pod 'ShortcutRecorder' #, :git => 'https://github.com/Kentzo/ShortcutRecorder', :branch => 'issue-148'
end
